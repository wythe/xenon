language: cpp
sudo: required
dist: bionic
install:
    - choco install make
    - choco install python3
    - choco install ninja
    - pip3 install meson
    #- make get-deps
notifications:
    email: false
compiler:
    - gcc
    - clang
os:
    #- linux
    #- osx
    - windows

script:
    - ./configure
    - make
    - make check
    - sudo make install
    - make check-install
    - sudo make uninstall
