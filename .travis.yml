language: cpp
sudo: required
dist: bionic
install:
    - choco install python3
    - choco install pip
    - pip3 install meson
    - choco install make
    - choco install ninja
    #- make get-deps
notifications:
    email: false
compiler:
    - gcc
    - clang
os:
    #- linux
    #- osx
    - windows

script:
    - ./configure
    - make
    - make check
    - sudo make install
    - make check-install
    - sudo make uninstall
