Boost_dep = dependency('Boost')
if 'Boost_FOUND'
  # include_directories(boost_include_dirs)
  decodeperf_exe = executable('decodeperf', 'decode.cpp')
  # target_link_libraries(['decodeperf', 'xenon'])
  # target_link_libraries(['decodeperf', boost_libraries])
endif
